#include <bits/stdc++.h>

using namespace std;
int main()
{
    int n;cin>>n;
    int cnt=1;
    if(n==1)
    cout<<"not prime";
    else{
    for(int i=2;i*i<=n;i++)
    {
        if(n%i==0)
        cnt=cnt+2;
    }
   // cout<<cnt;
    if(cnt>1)
    cout<<"not prime";
    else
    cout<<"prime";
    }
}
