#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    int k;cin>>k;
    auto it=find(arr,arr+n,k);
   int index=it-arr;
   if(index==n)
   cout<<"element not present";
   else
    cout<<index;
}
